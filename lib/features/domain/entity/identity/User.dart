
abstract class User {
  final String ? uid;
  final String ? name;
  final String ? email;
  final String ? sexual;
  final String ? phone;

  User({
    this.uid,
    this.name,
    this.email,
    this.sexual,
    this.phone
  });
}